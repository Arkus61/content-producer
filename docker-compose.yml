version: "3.8"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./experts:/app/experts

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
